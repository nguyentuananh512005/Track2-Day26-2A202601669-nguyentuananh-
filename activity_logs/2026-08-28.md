# Activity Log - 2026-08-28

## 10:35 - Vá 4 lỗ hổng bảo mật chí mạng (Defend Phase)
- **Files sửa đổi:**
  - `agent/gateway.py`
  - `agent/guardrails.py`
  - `agent/strategy.py`
  - `tests/test_agent_defend_inspector.py`
- **Chi tiết fix:**
  1. Fix `_act_owns_target` quét đệ quy mọi nested args và danh sách từ khóa mục tiêu, chặn Confused Deputy.
  2. Fix xử lý `fields` an toàn chống `TypeError` khi chứa `None` hoặc kiểu non-string trong `cheap_mask`, `ResultCache`, `is_catalog_trap`, `Command`, `Gateway`.
  3. Fix bẫy `live_leases` rỗng trong `slides.get_frame`, đảm bảo kiểm tra chặt chẽ `cmd.lease_id in live_leases`.
  4. Fix chuẩn hóa zero-width unicode chars và whitespace trước khi quét regex injection trong `guardrails.py`.
- **Kết quả kiểm thử:**
  - Unit test `test_agent_defend_inspector.py`: 6/6 PASSED (0.29s).
  - Toàn bộ test suite: 4,572 PASSED, 4 skipped (11.46s).

## 10:55 - Hoàn thiện Phase 3 (Attack Deck) & Phase 4 (Sparring & Submit)
- **Files sửa đổi:**
  - `deck/deck.json`: Hoàn thiện 14 lá bài (10 attack + 4 blank), 9 classes độc lập, phân bổ 4 MCP + 4 A2A + 2 Gateway. Cập nhật `defense_event = "gateway.denied"` và trỏ chính xác các `path_id` có drift thực tế (`8c5bf8f6`, `62fa4da7`).
  - `deck/lineup.json`: Lộ trình ra đòn 10 hiệp tối ưu.
  - `agent/gateway.py`: Bổ sung kiểm tra `x-server-fingerprint` (chống shadow server), `x-card-signature` (chống forged card), và `peer_unverified` (chống faithless peer).
  - `eval/prosecute.py`: Căn chỉnh `_hook_write_violation` và `_hook_protocol_misuse` để bỏ qua các lệnh đã bị Gateway từ chối, tránh recoil penalties.
- **Kết quả xác thực & kiểm thử:**
  - `python validate_deck.py`: **PASS 100%** (0 failing checks).
  - `python eval/prosecute.py`: **Precision = 1.000, Recall = 1.000, F1 = 1.000** trên 40 labelled fixtures.
  - `python spar.py --bot rookie`: **Thắng 100 - 0** (Knockout R6).
  - `python spar.py --bot operator`: **Thắng 100 - 0** (Knockout R6, chặn đứng Confused Deputy).
  - `python spar.py --bot adversary`: **Thắng 72 - 0 (Seed 1 KO R9), 48 - 19 (Seed 2), 72 - 13 (Seed 3)**.
  - Đóng gói Bundle: Tạo thành công `submissions/2A202601669_Nguyễn_Tuấn_Anh.bundle` (78,243 bytes, 13 files, 39 kit hashes).
